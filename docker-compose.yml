version: "3.8"

services:
  devkit-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: devkit-api
    restart: unless-stopped
    ports:
      - "4000:4000"
    environment:
      - NODE_ENV=production
      - PORT=4000
